// src/environments/environment.prod.ts
import { Environment } from './environment.d';

export const environment: Environment = {
  production: true,
  // IMPORTANTE: Substitua pela URL da sua API em produção
  apiUrl: 'https://sua-api.com/api/', 
};